{% extends 'admin/backend-layout.html.twig' %}

{% block title %}Service View{% endblock %}

{% block body %}
    <div class="text-color-grey mt-15">
        <h2>Team <small> view</small></h2>
    </div>
    <div class="mt-5 mb-5">
        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ services.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ services.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ services.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img src="{{ asset('image/' ~ services.servicePhoto) }}" width="100px" height="100px"></td>
            </tr>
            </tbody>
        </table>

    </div>
    <div class="text-center mt-15 ">
        <a href="{{ path('services_list') }}"><i class="fa-solid fa-left-long"></i>  Back to list</a>
    </div>
{% endblock %}