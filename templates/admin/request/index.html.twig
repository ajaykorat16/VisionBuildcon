{% extends 'admin/backend-layout.html.twig' %}

{% block body %}
    <div class="load-more-loader">
        {% include 'gif/loader-gif.html.twig' with {w: 44, h: 44} %}
    </div>
    <div class="text-color-grey mt-15">
        <h2>Request</h2>
    </div>
{#    <div class="form-group">#}
{#        {{ form_start(search,{'attr': {'id': 'search-form'}}) }}#}
{#        <div class=" d-flex" id="text-search">#}
{#            {{ form_widget(search.search, {'attr': {'class': 'form-control'}}) }}#}
{#            {{ form_widget(search.searchButton) }}#}
{#            <a href="{{ path('teams_list') }}"><i class="fa fa-refresh text-color-grey p-2"></i></a>#}
{#        </div>#}
{#        {{ form_end(search) }}#}
{#    </div>#}

{#    <div class="text-right mt-2">#}
{#        <a class="btn btn-primary" href="{{ path('teams_create') }}"><i class="fa-solid fa-plus"></i> Add</a>#}
{#    </div>#}
    <div class="pt-5">
        <table class="table table-bordered table-striped m-auto" id="request-list">
            <!-- Table header -->
            <thead>
            <tr class="text-color-grey text-center">
                <th>Id</th>
                <th>Name</th>
                <th>Email</th>
                <th>Subject</th>
                <th>Message</th>
{#                <th class="text-center">Action</th>#}
            </tr>
            </thead>
            <!-- Table body -->
            <tbody id="requestList" data-total-items="{{ totalRequests ?? 0 }}">
            {% if request is not empty %}
                {% for requests in request %}
                    {% include 'admin/request/list-items.html.twig' %}
                {% endfor %}
            {% else %}
                <tr>
                    <td colspan="3" class="no-records text-center" style="color: grey;">no records found</td>
                </tr>
            {% endif %}
            </tbody>
        </table>
    </div>
{% endblock %}